version: 1
nodes:
  - id: setup
    block: codex.setup
    codexHome: "{{ input.codexHome }}"
    mcpUrl: "{{ input.mcpUrl }}"
    dryRun: input.dryRun
    out: local.setup
  - id: done
    block: return
    value: "{{ local.setup }}"
