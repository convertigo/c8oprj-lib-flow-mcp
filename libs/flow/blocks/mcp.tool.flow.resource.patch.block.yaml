version: 1
name: mcp.tool.flow.resource.patch
private: true
icon: mdi:file-edit-outline
kind: mcp
description: Runs the flow-resource-patch MCP tool.
display: tool flow-resource-patch -> {{ props.out }}
props:
  request:
    kind: expression
    type: object
    default: input.request
    description: MCP JSON-RPC tools/call request object.
  out:
    kind: path
    mode: write
    default: local.response
    description: Scope path receiving the MCP response.
hooks:
  file: mcp.tool.flow.resource.patch.hooks.js
implementation:
  runtime: flow
  file: mcp.tool.flow.resource.patch.flow.yaml
