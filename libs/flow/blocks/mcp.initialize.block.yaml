version: 1
name: mcp.initialize
private: true
icon: mdi:hand-wave-outline
kind: mcp
description: Builds the MCP initialize response.
display: initialize
props:
  request:
    kind: expression
    type: object
    default: input.request
    description: MCP JSON-RPC request object.
  out:
    kind: path
    mode: write
    default: local.response
    description: Scope path receiving the MCP response.
hooks:
  file: mcp.initialize.hooks.js
implementation:
  runtime: flow
  file: mcp.initialize.flow.yaml
