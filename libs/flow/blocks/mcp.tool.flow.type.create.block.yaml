version: 1
name: mcp.tool.flow.type.create
private: true
icon: mdi:shape-plus-outline
kind: mcp
description: Runs the flow-type-create MCP tool.
display: tool flow-type-create -> {{ props.out }}
props:
  request:
    kind: expression
    type: object
    default: input.request
    description: MCP JSON-RPC tools/call request object.
  out:
    kind: path
    mode: write
    default: local.response
    description: Scope path receiving the MCP response.
implementation:
  runtime: flow
  file: mcp.tool.flow.type.create.flow.yaml
