version: 1
name: mcp.batch
private: true
icon: mdi:call-split
kind: mcp
description: Handles a JSON-RPC batch request.
display: batch -> {{ props.out }}
props:
  request:
    kind: expression
    type: array
    default: input.request
    description: MCP JSON-RPC request array.
  out:
    kind: path
    mode: write
    default: flow.response
    description: Scope path receiving the MCP batch response array.
hooks:
  file: mcp.batch.hooks.js
implementation:
  runtime: flow
  file: mcp.batch.flow.yaml
