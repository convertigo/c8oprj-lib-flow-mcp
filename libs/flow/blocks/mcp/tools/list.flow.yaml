version: 1
nodes:
  - id: availableTools
    block: mcp.tools.available
    out: local.tools
  - id: payload
    block: json.object
    out: local.payload
    fields:
      - id: tools
        block: json.field
        key: tools
        value: "{{ local.tools }}"
  - id: response
    block: mcp.response.result
    request: input.request
    result: local.payload
