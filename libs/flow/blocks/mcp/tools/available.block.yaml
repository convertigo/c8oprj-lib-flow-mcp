version: 1
private: true
icon: mdi:format-list-checks
tags:
- mcp
description: Returns the Flow MCP tool descriptors.
display: available tools -> {{ props.out }}
props:
  out:
    kind: path
    mode: write
    default: local.tools
    description: Scope path receiving the tool descriptors.
hooks:
  file: available.hooks.js
implementation:
  runtime: rhino
  file: available.js
