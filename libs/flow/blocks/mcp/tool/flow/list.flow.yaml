version: 1
nodes:
  - id: runFlowList
    block: mcp.tool.run
    request: props.request
    target: flow.list
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
