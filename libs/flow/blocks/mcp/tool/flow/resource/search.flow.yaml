version: 1
nodes:
  - id: runResourceSearch
    block: mcp.tool.run
    request: input.request
    target: resource.search
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
