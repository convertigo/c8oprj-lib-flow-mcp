version: 1
nodes:
  - id: runResourcePatch
    block: mcp.tool.run
    request: input.request
    target: resource.patch
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
