version: 1
nodes:
  - id: runBlockCreate
    block: mcp.tool.run
    request: input.request
    target: block.create
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
