version: 1
nodes:
  - id: testFlowBlock
    block: mcp.tool.run
    request: props.request
    target: flow.run
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
