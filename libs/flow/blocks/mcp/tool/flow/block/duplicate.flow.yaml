version: 1
nodes:
  - id: runBlockDuplicate
    block: mcp.tool.run
    request: input.request
    target: block.duplicate
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
