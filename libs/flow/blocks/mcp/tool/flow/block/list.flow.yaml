version: 1
nodes:
  - id: runBlockList
    block: mcp.tool.run
    request: input.request
    target: block.list
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
