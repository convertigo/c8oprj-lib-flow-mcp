version: 1
nodes:
  - id: runTypeList
    block: mcp.tool.run
    request: props.request
    target: type.list
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
