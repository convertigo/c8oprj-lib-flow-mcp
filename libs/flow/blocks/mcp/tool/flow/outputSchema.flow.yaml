version: 1
nodes:
  - id: readFlowOutputSchema
    block: mcp.tool.run
    request: input.request
    target: flow.outputSchema
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
