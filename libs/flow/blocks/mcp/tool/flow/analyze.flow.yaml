version: 1
nodes:
  - id: runFlowAnalyze
    block: mcp.tool.run
    request: input.request
    target: flow.analyze
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
