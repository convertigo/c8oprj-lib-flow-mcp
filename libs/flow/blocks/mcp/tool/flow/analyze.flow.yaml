version: 1
nodes:
  - id: runFlowAnalyze
    block: mcp.tool.run
    request: props.request
    target: flow.analyze
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
