version: 1
nodes:
  - id: applyFlowMutation
    block: mcp.tool.run
    request: input.request
    target: flow.apply
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
