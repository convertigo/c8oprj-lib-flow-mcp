version: 1
nodes:
  - id: resetFlowSchema
    block: mcp.tool.run
    request: props.request
    target: flow.schema.reset
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
