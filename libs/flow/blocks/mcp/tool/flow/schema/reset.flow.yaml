version: 1
nodes:
  - id: resetFlowSchema
    block: mcp.tool.run
    request: input.request
    target: flow.schema.reset
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
