version: 1
nodes:
  - id: runCatalog
    block: mcp.tool.run
    request: props.request
    target: block.list
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
