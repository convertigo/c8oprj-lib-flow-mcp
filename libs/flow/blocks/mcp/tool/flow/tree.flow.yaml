version: 1
nodes:
  - id: describeFlowTree
    block: mcp.tool.run
    request: input.request
    target: flow.tree
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
