version: 1
nodes:
  - id: describeFlowContext
    block: mcp.tool.run
    request: input.request
    target: flow.context
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
