version: 1
nodes:
  - id: describeFlowContext
    block: mcp.tool.run
    request: props.request
    target: flow.context
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
