version: 1
name: mcp.tool.flow.block.list
private: true
icon: mdi:puzzle-outline
kind: mcp
description: Runs the flow-block-list MCP tool.
display: tool flow-block-list -> {{ props.out }}
props:
  request:
    kind: expression
    type: object
    default: input.request
    description: MCP JSON-RPC tools/call request object.
  out:
    kind: path
    mode: write
    default: local.response
    description: Scope path receiving the MCP response.
implementation:
  runtime: flow
  file: mcp.tool.flow.block.list.flow.yaml
