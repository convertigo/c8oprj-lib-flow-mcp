version: 1
nodes:
  - id: runBlockGet
    block: mcp.tool.run
    request: props.request
    target: block.get
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
