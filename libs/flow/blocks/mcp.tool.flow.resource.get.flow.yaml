version: 1
nodes:
  - id: runResourceGet
    block: mcp.tool.run
    request: props.request
    target: resource.get
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
