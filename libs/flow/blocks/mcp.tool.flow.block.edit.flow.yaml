version: 1
nodes:
  - id: runBlockEdit
    block: mcp.tool.run
    request: props.request
    target: block.edit
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
