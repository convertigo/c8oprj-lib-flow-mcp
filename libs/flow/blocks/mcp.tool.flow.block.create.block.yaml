version: 1
name: mcp.tool.flow.block.create
private: true
icon: mdi:puzzle-plus-outline
kind: mcp
description: Runs the flow-block-create MCP tool.
display: tool flow-block-create -> {{ props.out }}
props:
  request:
    kind: expression
    type: object
    default: input.request
    description: MCP JSON-RPC tools/call request object.
  out:
    kind: path
    mode: write
    default: local.response
    description: Scope path receiving the MCP response.
nodes:
  - id: runBlockCreate
    block: mcp.tool.run
    request: props.request
    target: block.create
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
