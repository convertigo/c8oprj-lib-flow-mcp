version: 1
nodes:
  - id: error
    block: mcp.response.error
    request: props.request
    code: -32601
    message: "\"Method not found: \" + (props.request.method || \"\")"
    out: local.response
  - id: done
    block: return
    value: "{{ local.response }}"
